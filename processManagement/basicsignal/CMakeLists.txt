cmake_minimum_required (VERSION 2.6)
project(basicsignal)
enable_testing()
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/bin")
add_executable(basicsignal basicsignal.c)
add_test(NAME basicsignal COMMAND  basicsignal WORKING_DIERCTORY ${CMAKE_CURRENT_SOURCE_DIR}/bin)
